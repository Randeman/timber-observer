import { FormatedDataPipe } from './formated-data.pipe';

describe('FormatedDataPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatedDataPipe();
    expect(pipe).toBeTruthy();
  });
});
